_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
Gemfile.lock
.DS_Store

# Internal working docs that must NOT be published (named prospects, contacts).
# Kept out of the public operating record for privacy (R-06).
_private/
